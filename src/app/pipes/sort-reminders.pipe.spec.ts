import { SortRemindersPipe } from './sort-reminders.pipe';

describe('SortRemindersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRemindersPipe();
    expect(pipe).toBeTruthy();
  });
});
