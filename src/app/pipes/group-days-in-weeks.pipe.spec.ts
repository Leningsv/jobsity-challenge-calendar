import { GroupDaysInWeeksPipe } from './group-days-in-weeks.pipe';

describe('GroupDaysInWeeksPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupDaysInWeeksPipe();
    expect(pipe).toBeTruthy();
  });
});
